name: Publish Docker images

on:
  push:
    branches: [ "main", "master" ]

env:
  REGISTRY: ghcr.io
  # Приводим имя пользователя к нижнему регистру для совместимости с Docker
  IMAGE_PREFIX: ghcr.io/${{ github.repository_owner }}

jobs:
  build-and-push-images:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write # Важно: дает права на загрузку в GitHub Container Registry

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Log in to the Container registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Lowercase image prefix
        run: |
          echo "IMAGE_PREFIX=${IMAGE_PREFIX,,}" >> ${GITHUB_ENV}

      - name: Build images
        run: |
          docker build -t "${IMAGE_PREFIX}/status-dashboard-api:latest" ./api
          docker build -t "${IMAGE_PREFIX}/status-dashboard-web:latest" ./web
          docker build -t "${IMAGE_PREFIX}/status-dashboard-web-demo:latest" -f ./web/Dockerfile.demo ./web

      - name: Smoke test images
        run: |
          docker run --rm --entrypoint node "${IMAGE_PREFIX}/status-dashboard-api:latest" \
            --input-type=module -e "await import('./src/server.js'); setTimeout(() => process.exit(0), 100)"
          docker run --rm --add-host status-dashboard-api:127.0.0.1 \
            --entrypoint nginx "${IMAGE_PREFIX}/status-dashboard-web:latest" -t
          docker run --rm --entrypoint nginx "${IMAGE_PREFIX}/status-dashboard-web-demo:latest" -t

      - name: Push images
        run: |
          push_image() {
            local image="$1"
            for attempt in 1 2 3; do
              echo "Pushing ${image} (attempt ${attempt}/3)"
              if docker push "$image"; then
                return 0
              fi
              echo "Push failed for ${image}; retrying after registry backoff..."
              sleep "$((attempt * 20))"
            done

            echo "Push failed for ${image} after 3 attempts"
            return 1
          }

          push_image "${IMAGE_PREFIX}/status-dashboard-api:latest"
          push_image "${IMAGE_PREFIX}/status-dashboard-web:latest"
          push_image "${IMAGE_PREFIX}/status-dashboard-web-demo:latest"
